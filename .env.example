# Copy to .env on the hardware laptop and fill in the real secret.
ANTHROPIC_API_KEY=

# Optional override if you do not pass --config.
HWEXEC_CONFIG=config.yaml

# Optional server overrides for `hwexec serve`.
HWEXEC_HOST=127.0.0.1
HWEXEC_PORT=8765
